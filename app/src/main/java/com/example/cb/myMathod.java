package com.example.cb;

import java.util.ArrayList;

public class myMathod {
     public static String deletListChar(String string){
         String reasult="";
         for (int i=0;i<string.length()-1;i++){
             reasult+=string.charAt(i);
         }
         return reasult;
    }
    public static double findResult(String M){
        ArrayList<Integer> nu=new ArrayList<Integer>();
        for (int i=0;i<M.length();i++){
            if(M.charAt(i)!='+'){

            }
        }
         return 1.0;
    }
}
